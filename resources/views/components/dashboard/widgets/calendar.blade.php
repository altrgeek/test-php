@props(['calendarId' => 'calendar'])
<div class="card card-bordered">
    <div class="card-inner">
        <!-- Custom calendar -->
        <div id="{{ $calendarId }}" class="nk-calendar"></div>
    </div>
</div>
